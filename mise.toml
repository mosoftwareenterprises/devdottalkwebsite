[tools]
node = '24'
gh = '2'

[tasks.build]
run = ["npm install", "npm ci", "npm run build"]

[tasks.test]
run = "npm run test"
depends = "build"

[tasks.start]
run = ["npx kill-port 8080 || true", "npm start"]
