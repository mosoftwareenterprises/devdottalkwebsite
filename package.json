{
  "name": "devdottalk-site",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0"
  },
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "preview": "sirv _site --single --cors --no-clear --port 8080"
  }
}
